{# Importer fichier #}

{% extends 'base.html.twig' %}

{% block container %}

    <div class="container">
        <div class="row">

            {% for article in articles %}

                <div class="card mt-3" style="width: 18rem; margin-right: 2rem;">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.name }}</h5>
                        <p class="card-text">Catégorie : <b>{{ article.category.label }}</b></p>
                        <p class="card-text">{{ article.content }}</p>
                        <a href="#" class="card-link">Card link</a>
                        <a href="#" class="card-link">Another link</a>
                    </div>
                </div>

            {% endfor %}

        </div>
    </div>

{% endblock %}